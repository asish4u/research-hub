Laws: add in-progress bills, status filters, 1-year window, attention flags

- /api/laws now returns enacted laws AND bills still in progress (passed at least one chamber) from the past 12 months, deduped and sorted by latest action
- Add per-query timeout so a slow GovTrack call cannot block the endpoint
- UI: new status filter row — All statuses / Enacted / In progress / Needs attention — combining with the existing relevance filters
- Each item flagged for attention: Needs attention (pending, passed a chamber) or New law (enacted under 30 days)
- Header/legend/README updated for the 12-month enacted + in-progress scope

🤖 Generated with Codebuff
Co-Authored-By: Codebuff <noreply@codebuff.com>
